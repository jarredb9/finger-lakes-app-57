# Bidirectional Friendship Social Model

The social layer is strictly modeled around mutual, bidirectional Friends established via explicit Friend Requests, rather than asymmetric following. This aligns with collaborative trip planning and intimate visit sharing among peers, eliminates the overhead of managing separate follower/following graphs, and renders legacy database follow tables deprecated.
